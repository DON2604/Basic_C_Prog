#include<stdio.h>
void main()
{
    int a,b,c,d,x;
    scanf("%d %d %d %d %d", &a,&b,&c,&d,&x);
    if(a==x)
    printf("%d is matched one",a);
    if(b==x)
    printf("%d is matched one",b);
    if(c==x)
    printf("%d is matched one",c);
    if(d==x)
    printf("%d is matched one",d);

    
}